// TN619 — الأعداد العشرية والتحويل (Arabic surface)
دالة مساحة_دائرة(نق: عائم) -> عائم {
    ٣.١٤١٥٩ * نق * نق
}

دالة رئيسي() {
    دع نق = ٢.٠
    اطبع("المساحة:"، مساحة_دائرة(نق))

    دع نصف = ٧ كـ عائم / ٢.٠
    اطبع("٧/٢ عشري:"، نصف)
}
